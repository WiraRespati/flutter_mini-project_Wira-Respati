import 'package:flutter/material.dart';

class HeaderDetailWidget extends StatelessWidget {
  const HeaderDetailWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text("Detail Story");
  }
}